[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "securepdf"
version = "0.0.1"
description = "Minimal Python SDK for SecurePDF"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
